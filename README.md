# Resource Server

This project is a Spring Boot OAuth2 Resource Server that validates JWT access tokens
issued by a custom OAuth2 Authorization Server and protects REST APIs using role-based authorization.
